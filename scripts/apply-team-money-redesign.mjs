import { readFileSync, writeFileSync } from "node:fs";

function replaceOnce(source, before, after, label) {
  if (!source.includes(before)) throw new Error(`Trecho não encontrado: ${label}`);
  return source.replace(before, after);
}

const teamMoneyPath = "db/team-money.ts";
let teamMoney = readFileSync(teamMoneyPath, "utf8");

teamMoney = replaceOnce(
  teamMoney,
  'import { appDate } from "../lib/app-date";',
  'import { appDate } from "../lib/app-date";\nimport { listPublicGalleryImages } from "./public-gallery";',
  "import galeria no ledger",
);

teamMoney = replaceOnce(
  teamMoney,
  `  role: string;\n  paymentDay: number;\n  earnedCents: number;`,
  `  role: string;\n  photoUrl: string | null;\n  paymentDay: number;\n  earnedCents: number;`,
  "foto no TeamMoneyRow",
);

const memberQueryBefore = `  const memberRows = await db.select({
    id: team.id,
    name: team.name,
    role: team.role,
    accessRole: team.accessRole,
    paymentDay: team.paymentDay,
  }).from(team).where(eq(team.organizationId, access.organizationId)).orderBy(team.name);

  const visibleMembers = memberRows
    .filter((member) => member.accessRole !== "owner")
    .filter((member) => access.isOwner || member.id === access.teamMemberId);`;

const memberQueryAfter = `  const [memberRows, gallery] = await Promise.all([
    db.select({
      id: team.id,
      name: team.name,
      role: team.role,
      accessRole: team.accessRole,
      paymentDay: team.paymentDay,
    }).from(team).where(eq(team.organizationId, access.organizationId)).orderBy(team.name),
    listPublicGalleryImages(access.organizationId),
  ]);
  const photoByMember = new Map(
    gallery
      .filter((image) => image.kind === "barber" && image.teamMemberId)
      .map((image) => [Number(image.teamMemberId), image.url] as const),
  );

  const visibleMembers = memberRows
    .filter((member) => access.isOwner || member.id === access.teamMemberId);`;

teamMoney = replaceOnce(teamMoney, memberQueryBefore, memberQueryAfter, "incluir proprietário e fotos");

teamMoney = replaceOnce(
  teamMoney,
  `      role: member.role,\n      paymentDay: member.paymentDay,`,
  `      role: member.role,\n      photoUrl: photoByMember.get(member.id) ?? null,\n      paymentDay: member.paymentDay,`,
  "foto no retorno",
);

teamMoney = replaceOnce(
  teamMoney,
  `      currentBalanceCents: cycle.currentBalanceCents,\n      openRecordCount: cycle.records.length,`,
  `      currentBalanceCents: cycle.currentBalanceCents,\n      openRecordCount: cycle.records.length,\n      hasOpenActivity: cycle.records.length > 0 || cycle.sales.length > 0 || cycle.entries.length > 0,`,
  "atividade aberta",
);

teamMoney = replaceOnce(
  teamMoney,
  `  currentBalanceCents: number;\n  openRecordCount: number;`,
  `  currentBalanceCents: number;\n  openRecordCount: number;\n  hasOpenActivity: boolean;`,
  "tipo atividade aberta",
);

teamMoney = replaceOnce(
  teamMoney,
  `    eq(team.id, teamMemberId),\n    eq(team.organizationId, access.organizationId),\n    eq(team.accessRole, "barber"),\n  )).returning({ id: team.id });`,
  `    eq(team.id, teamMemberId),\n    eq(team.organizationId, access.organizationId),\n  )).returning({ id: team.id });`,
  "dia de fechamento do proprietário",
);

teamMoney = replaceOnce(
  teamMoney,
  '  if (!member || member.accessRole === "owner") throw new Error("Escolha um funcionário válido.");',
  '  if (!member) throw new Error("Escolha um profissional válido.");',
  "fechamento do proprietário",
);

teamMoney = teamMoney.replaceAll("Registre um novo vale ou pagamento.", "Registre um novo vale.");
writeFileSync(teamMoneyPath, teamMoney);

writeFileSync("app/ui/team-money-section.tsx", "\"use client\";\n\nimport { FormEvent, useEffect, useMemo, useState } from \"react\";\nimport type { TeamMoneyData, TeamMoneyEntry, TeamMoneyRow } from \"../../db/team-money\";\nimport { showAppToast } from \"./app-toast\";\nimport { AppIcon } from \"./app-icon\";\n\ntype Post = (body: Record<string, string | number | boolean>, success: string) => Promise<boolean>;\n\nconst money = (cents: number) => new Intl.NumberFormat(\"pt-BR\", { style: \"currency\", currency: \"BRL\" }).format(Number(cents || 0) / 100);\nconst date = (value: string) => new Date(`${value}T12:00:00`).toLocaleDateString(\"pt-BR\");\nconst today = () => {\n  const formatter = new Intl.DateTimeFormat(\"en-CA\", { timeZone: \"America/Sao_Paulo\", year: \"numeric\", month: \"2-digit\", day: \"2-digit\" });\n  return formatter.format(new Date());\n};\n\nfunction paymentDayLabel(day: number) {\n  return day === 31 ? \"último dia do mês\" : `dia ${day}`;\n}\n\nfunction initials(value: string) {\n  return value.split(/\\s+/).filter(Boolean).slice(0, 2).map((part) => part[0]?.toUpperCase()).join(\"\") || \"CA\";\n}\n\nfunction MemberAvatar({ row, large = false }: { row: TeamMoneyRow; large?: boolean }) {\n  if (row.photoUrl) return <img className={`team-money-v2-avatar${large ? \" large\" : \"\"}`} src={row.photoUrl} alt={`Foto de ${row.teamMemberName}`} loading=\"lazy\" />;\n  return <span className={`team-money-v2-avatar fallback${large ? \" large\" : \"\"}`}>{initials(row.teamMemberName)}</span>;\n}\n\nfunction pdfFilename(response: Response, id: number) {\n  const disposition = response.headers.get(\"content-disposition\") ?? \"\";\n  const utf8 = disposition.match(/filename\\*=UTF-8''([^;]+)/i)?.[1];\n  if (utf8) {\n    try {\n      return decodeURIComponent(utf8.replace(/[\"']/g, \"\"));\n    } catch {\n      // Se o nome vier malformado, usa o nome simples abaixo.\n    }\n  }\n  const plain = disposition.match(/filename=\"?([^\";]+)\"?/i)?.[1]?.trim();\n  return plain || `fechamento-${id}.pdf`;\n}\n\nfunction isAppleMobileDevice() {\n  return /iPad|iPhone|iPod/.test(navigator.userAgent)\n    || (navigator.platform === \"MacIntel\" && navigator.maxTouchPoints > 1);\n}\n\nasync function downloadPdf(id: number) {\n  try {\n    const response = await fetch(`/api/team-money/closures/${id}/pdf`, { cache: \"no-store\" });\n    if (response.status === 401) {\n      window.location.assign(\"/\");\n      return;\n    }\n    if (!response.ok) {\n      let message = \"Não foi possível baixar o PDF agora.\";\n      try {\n        const payload = await response.json() as { error?: string };\n        if (payload.error) message = payload.error;\n      } catch {\n        // Mantém a mensagem padrão quando a resposta não for JSON.\n      }\n      throw new Error(message);\n    }\n\n    const blob = await response.blob();\n    const filename = pdfFilename(response, id);\n    const file = new File([blob], filename, { type: \"application/pdf\" });\n    const shareData: ShareData = { files: [file], title: \"Fechamento do Cortou Anotou\" };\n\n    if (\n      isAppleMobileDevice()\n      && typeof navigator.share === \"function\"\n      && typeof navigator.canShare === \"function\"\n      && navigator.canShare(shareData)\n    ) {\n      try {\n        showAppToast(\"No iPhone, escolha Salvar em Arquivos para guardar o PDF.\");\n        await navigator.share(shareData);\n        return;\n      } catch (error) {\n        if (error instanceof DOMException && error.name === \"AbortError\") return;\n      }\n    }\n\n    const downloadBlob = new Blob([blob], { type: \"application/octet-stream\" });\n    const url = URL.createObjectURL(downloadBlob);\n    const link = document.createElement(\"a\");\n    link.href = url;\n    link.download = filename;\n    link.rel = \"noopener\";\n    document.body.appendChild(link);\n    link.click();\n    link.remove();\n    window.setTimeout(() => URL.revokeObjectURL(url), 1500);\n  } catch (error) {\n    showAppToast(error instanceof Error ? error.message : \"Não foi possível baixar o PDF agora.\");\n  }\n}\n\nasync function fetchTeamMoneyData() {\n  const response = await fetch(\"/api/team-money\", { cache: \"no-store\" });\n  if (response.status === 401) {\n    window.location.assign(\"/\");\n    return null;\n  }\n  const payload = await response.json() as { data?: TeamMoneyData; error?: string };\n  if (!response.ok || !payload.data) throw new Error(payload.error ?? \"Não foi possível carregar os valores da equipe.\");\n  return payload.data;\n}\n\nexport function TeamMoneySection({ owner, post, pending: outerPending = false }: { owner: boolean; post?: Post; pending?: boolean }) {\n  const [data, setData] = useState<TeamMoneyData | null>(null);\n  const [loading, setLoading] = useState(true);\n  const [feedback, setFeedback] = useState<string | null>(null);\n  const [pending, setPending] = useState(false);\n  const [selectedMemberId, setSelectedMemberId] = useState<number | null>(null);\n  const [editing, setEditing] = useState<TeamMoneyEntry | null>(null);\n  const [valeFormOpen, setValeFormOpen] = useState(false);\n\n  async function load() {\n    try {\n      const nextData = await fetchTeamMoneyData();\n      if (!nextData) return;\n      setData(nextData);\n      setFeedback(null);\n    } catch (error) {\n      setFeedback(error instanceof Error ? error.message : \"Não foi possível carregar agora.\");\n    } finally {\n      setLoading(false);\n    }\n  }\n\n  useEffect(() => {\n    let active = true;\n    void fetchTeamMoneyData()\n      .then((nextData) => {\n        if (!active || !nextData) return;\n        setData(nextData);\n        setFeedback(null);\n      })\n      .catch((error) => {\n        if (active) setFeedback(error instanceof Error ? error.message : \"Não foi possível carregar agora.\");\n      })\n      .finally(() => {\n        if (active) setLoading(false);\n      });\n    return () => { active = false; };\n  }, []);\n\n  const rows = data?.rows ?? [];\n\n  useEffect(() => {\n    if (!owner || !rows.length) return;\n    if (selectedMemberId === null || !rows.some((row) => row.teamMemberId === selectedMemberId)) {\n      setSelectedMemberId(rows[0].teamMemberId);\n    }\n  }, [owner, rows, selectedMemberId]);\n\n  const selectedRow = owner\n    ? rows.find((row) => row.teamMemberId === selectedMemberId) ?? rows[0]\n    : rows[0];\n\n  const selectedClosures = useMemo(\n    () => data?.closures.filter((closure) => !closure.isBaseline && (!selectedRow || closure.teamMemberId === selectedRow.teamMemberId)) ?? [],\n    [data?.closures, selectedRow],\n  );\n\n  async function teamMoneyAction(body: Record<string, string | number | boolean>) {\n    setPending(true);\n    setFeedback(null);\n    try {\n      const response = await fetch(\"/api/team-money\", {\n        method: \"POST\",\n        headers: { \"content-type\": \"application/json\" },\n        body: JSON.stringify(body),\n      });\n      const payload = await response.json() as { data?: TeamMoneyData; error?: string; closureId?: number };\n      if (!response.ok || !payload.data) throw new Error(payload.error ?? \"Não foi possível concluir.\");\n      setData(payload.data);\n      return payload;\n    } catch (error) {\n      setFeedback(error instanceof Error ? error.message : \"Não foi possível concluir.\");\n      return null;\n    } finally {\n      setPending(false);\n    }\n  }\n\n  async function savePaymentDay(event: FormEvent<HTMLFormElement>, row: TeamMoneyRow) {\n    event.preventDefault();\n    const form = new FormData(event.currentTarget);\n    const result = await teamMoneyAction({ action: \"save-payment-day\", teamMemberId: row.teamMemberId, paymentDay: Number(form.get(\"paymentDay\")) });\n    if (result) showAppToast(`Dia previsto de ${row.teamMemberName} atualizado.`);\n  }\n\n  async function closeCycle(row: TeamMoneyRow) {\n    if (!window.confirm(`Fechar o período de ${row.teamMemberName} em ${money(row.currentBalanceCents)}? O valor atual será registrado como recebido e o próximo período começará zerado.`)) return;\n    const result = await teamMoneyAction({ action: \"close\", teamMemberId: row.teamMemberId });\n    if (!result?.closureId) return;\n    showAppToast(\"Período fechado. O saldo atual voltou para R$ 0,00 e o PDF está pronto.\");\n  }\n\n  async function submitVale(event: FormEvent<HTMLFormElement>) {\n    event.preventDefault();\n    if (!post || !selectedRow) return;\n    const form = new FormData(event.currentTarget);\n    const ok = await post({\n      action: \"team-payment\",\n      id: editing?.id ?? 0,\n      teamMemberId: selectedRow.teamMemberId,\n      occurredAt: String(form.get(\"occurredAt\") ?? today()),\n      kind: \"Vale\",\n      reason: String(form.get(\"reason\") ?? \"\"),\n      valueCents: Math.round(Number(form.get(\"value\") ?? 0) * 100),\n    }, editing ? \"Vale atualizado.\" : `Vale registrado para ${selectedRow.teamMemberName}.`);\n    if (ok) {\n      setEditing(null);\n      setValeFormOpen(false);\n      await load();\n    }\n  }\n\n  async function removeEntry(entry: TeamMoneyEntry) {\n    if (!post || entry.kind !== \"Vale\") return;\n    if (!window.confirm(`Excluir vale de ${money(entry.valueCents)} de ${entry.teamMemberName}?`)) return;\n    const ok = await post({ action: \"delete-team-payment\", id: entry.id }, \"Vale excluído.\");\n    if (ok) {\n      if (editing?.id === entry.id) setEditing(null);\n      await load();\n    }\n  }\n\n  function openNewVale() {\n    setEditing(null);\n    setValeFormOpen(true);\n  }\n\n  function editVale(entry: TeamMoneyEntry) {\n    if (entry.kind !== \"Vale\") return;\n    setEditing(entry);\n    setValeFormOpen(true);\n  }\n\n  if (loading) return <section className=\"panel team-money-loading\">Carregando valores da equipe...</section>;\n\n  if (!owner) {\n    const row = rows[0];\n    return <div className=\"team-money-shell team-money-v2\">\n      {feedback && <div className=\"notice error\">{feedback}</div>}\n      {row ? <>\n        <section className=\"team-money-v2-intro\">\n          <div><span>MINHA GRANA</span><h2>Meu saldo</h2><p>Acompanhe o que você já ganhou, os vales retirados e o próximo fechamento.</p></div>\n          <MemberAvatar row={row} large />\n        </section>\n        <section className=\"panel team-money-v2-focus\">\n          <header className=\"team-money-v2-person\">\n            <MemberAvatar row={row} large />\n            <div><h2>{row.teamMemberName}</h2><small>{row.role}</small></div>\n          </header>\n          <div className=\"team-money-v2-summary\">\n            <article className=\"receive\"><span><AppIcon name=\"money\" /></span><div><small>A RECEBER</small><strong>{money(row.currentBalanceCents)}</strong><em>saldo atual</em></div></article>\n            <article><span><AppIcon name=\"scissors\" /></span><div><small>COMISSÃO GERADA</small><strong>{money(row.earnedCents)}</strong><em>desde o último fechamento</em></div></article>\n            <article><span><AppIcon name=\"money\" /></span><div><small>VALES</small><strong>{money(row.valeCents)}</strong><em>já descontados</em></div></article>\n            <article><span><AppIcon name=\"calendar\" /></span><div><small>FECHAMENTO PREVISTO</small><strong>{paymentDayLabel(row.paymentDay)}</strong><em>{row.lastClosure ? `Último: ${date(row.lastClosure.periodEndDate)}` : \"Sem fechamento anterior\"}</em></div></article>\n          </div>\n        </section>\n        <section className=\"panel team-money-v2-movements\">\n          <div className=\"team-money-v2-heading\"><div><span>EM ABERTO</span><h3>Vales deste período</h3></div><b>{row.openEntries.filter((entry) => entry.kind === \"Vale\").length}</b></div>\n          <div className=\"team-money-v2-list\">\n            {row.openEntries.filter((entry) => entry.kind === \"Vale\").map((entry) => <article key={entry.id}><i className=\"vale\"><AppIcon name=\"money\" /></i><div><strong>Vale</strong><small>{date(entry.occurredAt)} · {entry.reason}</small></div><b>- {money(entry.valueCents)}</b></article>)}\n            {!row.openEntries.some((entry) => entry.kind === \"Vale\") && <p className=\"team-money-empty\">Nenhum vale em aberto neste período.</p>}\n          </div>\n        </section>\n        {data && <ClosureHistory data={data} memberId={row.teamMemberId} />}\n      </> : <section className=\"panel team-money-loading\">Seu cadastro ainda não possui saldo disponível.</section>}\n    </div>;\n  }\n\n  if (!selectedRow) return <section className=\"panel team-money-loading\">Cadastre a equipe para controlar vales e fechamentos.</section>;\n\n  const visibleEntries = selectedRow.openEntries.slice().sort((a, b) => b.occurredAt.localeCompare(a.occurredAt) || b.id - a.id);\n  const openValeCount = visibleEntries.filter((entry) => entry.kind === \"Vale\").length;\n\n  return <div className=\"team-money-shell team-money-v2\">\n    {feedback && <div className=\"notice error\">{feedback}</div>}\n\n    <section className=\"team-money-v2-intro\">\n      <div><span>CONTROLE DA EQUIPE</span><h2>Vales e fechamentos</h2><p>Veja quanto cada profissional tem para receber, registre vales e feche o período sem fazer conta por fora.</p></div>\n    </section>\n\n    <nav className=\"team-money-v2-tabs\" aria-label=\"Escolher profissional\">\n      {rows.map((row) => <button type=\"button\" key={row.teamMemberId} className={selectedRow.teamMemberId === row.teamMemberId ? \"active\" : \"\"} onClick={() => { setSelectedMemberId(row.teamMemberId); setValeFormOpen(false); setEditing(null); }}>\n        <MemberAvatar row={row} />\n        <span><strong>{row.teamMemberName}</strong><small>{money(row.currentBalanceCents)} a receber</small></span>\n      </button>)}\n    </nav>\n\n    <section className=\"panel team-money-v2-focus\">\n      <header className=\"team-money-v2-person\">\n        <MemberAvatar row={selectedRow} large />\n        <div><h2>{selectedRow.teamMemberName}</h2><small>{selectedRow.role}</small></div>\n        <span className=\"team-money-v2-status\">Em aberto</span>\n      </header>\n\n      <div className=\"team-money-v2-summary\">\n        <article className=\"receive\"><span><AppIcon name=\"money\" /></span><div><small>A RECEBER</small><strong>{money(selectedRow.currentBalanceCents)}</strong><em>já descontados os vales</em></div></article>\n        <article><span><AppIcon name=\"money\" /></span><div><small>VALES</small><strong>{money(selectedRow.valeCents)}</strong><em>{openValeCount} {openValeCount === 1 ? \"vale\" : \"vales\"} no período</em></div></article>\n        <article><span><AppIcon name=\"scissors\" /></span><div><small>COMISSÃO GERADA</small><strong>{money(selectedRow.earnedCents)}</strong><em>serviços, produtos e gorjetas</em></div></article>\n        <article><span><AppIcon name=\"calendar\" /></span><div><small>ÚLTIMO FECHAMENTO</small><strong>{selectedRow.lastClosure ? money(selectedRow.lastClosure.settlementCents) : \"—\"}</strong><em>{selectedRow.lastClosure ? date(selectedRow.lastClosure.periodEndDate) : \"Ainda não realizado\"}</em></div></article>\n      </div>\n\n      <div className=\"team-money-v2-actions\">\n        <button className=\"vale-action\" type=\"button\" disabled={pending || outerPending} onClick={openNewVale}><AppIcon name=\"money\" /> Novo vale</button>\n        <button className=\"close-action\" type=\"button\" disabled={pending || outerPending || !selectedRow.hasOpenActivity} onClick={() => void closeCycle(selectedRow)}><AppIcon name=\"check\" /> Fechar período</button>\n      </div>\n\n      <form className=\"team-money-v2-day\" onSubmit={(event) => savePaymentDay(event, selectedRow)} key={`day-${selectedRow.teamMemberId}`}>\n        <label>Dia previsto de fechamento <input name=\"paymentDay\" type=\"number\" min=\"1\" max=\"31\" defaultValue={selectedRow.paymentDay} /></label>\n        <button type=\"submit\" disabled={pending || outerPending}>Salvar</button>\n      </form>\n    </section>\n\n    {valeFormOpen && <section className=\"panel team-money-v2-vale-form\">\n      <div className=\"team-money-v2-heading\"><div><span>{editing ? \"EDITANDO VALE\" : \"NOVO VALE\"}</span><h3>{selectedRow.teamMemberName}</h3></div><button type=\"button\" className=\"quiet-close\" onClick={() => { setValeFormOpen(false); setEditing(null); }}>Fechar</button></div>\n      <form onSubmit={submitVale} key={editing?.id ?? `new-${selectedRow.teamMemberId}`}>\n        <label>Data<input name=\"occurredAt\" type=\"date\" defaultValue={editing?.occurredAt ?? today()} required /></label>\n        <label>Valor (R$)<input name=\"value\" type=\"number\" min=\"0.01\" step=\"0.01\" inputMode=\"decimal\" defaultValue={editing ? editing.valueCents / 100 : undefined} placeholder=\"0,00\" required /></label>\n        <label className=\"reason-field\">Motivo<input name=\"reason\" maxLength={160} placeholder=\"Ex.: adiantamento, almoço, transporte\" defaultValue={editing?.reason ?? \"\"} required /></label>\n        <button className=\"primary-button\" disabled={pending || outerPending}>{editing ? \"Salvar vale\" : \"Registrar vale\"}</button>\n      </form>\n    </section>}\n\n    <section className=\"panel team-money-v2-movements\">\n      <div className=\"team-money-v2-heading\"><div><span>MOVIMENTAÇÕES</span><h3>Desde o último fechamento</h3></div><b>{visibleEntries.length} lançamento{visibleEntries.length === 1 ? \"\" : \"s\"}</b></div>\n      <div className=\"team-money-v2-list\">\n        {visibleEntries.map((entry) => {\n          const isVale = entry.kind === \"Vale\";\n          return <article key={entry.id}>\n            <i className={isVale ? \"vale\" : \"legacy\"}>{isVale ? <AppIcon name=\"money\" /> : <AppIcon name=\"check\" />}</i>\n            <div><strong>{isVale ? \"Vale\" : \"Ajuste antigo\"}</strong><small>{date(entry.occurredAt)} · {entry.reason}</small></div>\n            <b>- {money(entry.valueCents)}</b>\n            {isVale && <div className=\"team-money-v2-entry-actions\"><button type=\"button\" onClick={() => editVale(entry)}>Editar</button><button type=\"button\" className=\"danger\" disabled={pending || outerPending} onClick={() => void removeEntry(entry)}>Excluir</button></div>}\n          </article>;\n        })}\n        {!visibleEntries.length && <p className=\"team-money-empty\">Nenhum vale em aberto. As novas movimentações vão aparecer aqui.</p>}\n      </div>\n    </section>\n\n    <ClosureHistory data={data!} memberId={selectedRow.teamMemberId} closures={selectedClosures} />\n  </div>;\n}\n\nfunction ClosureHistory({ data, memberId, closures }: { data: TeamMoneyData; memberId?: number; closures?: TeamMoneyData[\"closures\"] }) {\n  const visible = closures ?? data.closures.filter((closure) => !closure.isBaseline && (!memberId || closure.teamMemberId === memberId));\n  return <section className=\"panel team-money-v2-history\">\n    <div className=\"team-money-v2-heading\"><div><span>HISTÓRICO</span><h3>Fechamentos concluídos</h3></div><b>{visible.length}</b></div>\n    <div className=\"team-money-v2-history-list\">\n      {visible.map((closure) => <article key={closure.id}><div><strong>{closure.teamMemberName}</strong><small>{date(closure.periodStartDate)} a {date(closure.periodEndDate)} · {closure.recordCount} atendimento(s)</small></div><b>{money(closure.settlementCents)}</b><button type=\"button\" onClick={() => void downloadPdf(closure.id)}>PDF</button></article>)}\n      {!visible.length && <p className=\"team-money-empty\">O primeiro fechamento deste profissional vai aparecer aqui.</p>}\n    </div>\n  </section>;\n}\n");

const dashboardPath = "app/ui/dashboard-app.tsx";
let dashboard = readFileSync(dashboardPath, "utf8");

dashboard = replaceOnce(
  dashboard,
  `  const [showAllPayouts, setShowAllPayouts] = useState(false);\n  const data = usePeriodDashboardData(baseData, start, end);`,
  `  const [showAllPayouts, setShowAllPayouts] = useState(false);
  const [teamMoneyRows, setTeamMoneyRows] = useState<Array<{ teamMemberId: number; teamMemberName: string; currentBalanceCents: number; photoUrl: string | null }>>([]);
  useEffect(() => {
    let active = true;
    const loadBalances = () => {
      void fetch("/api/team-money", { cache: "no-store" })
        .then(async (response) => {
          const payload = await response.json() as { data?: { rows?: Array<{ teamMemberId: number; teamMemberName: string; currentBalanceCents: number; photoUrl: string | null }> } };
          if (active && response.ok) setTeamMoneyRows(payload.data?.rows ?? []);
        })
        .catch(() => undefined);
    };
    loadBalances();
    const refresh = () => { if (document.visibilityState === "visible") loadBalances(); };
    window.addEventListener("focus", loadBalances);
    document.addEventListener("visibilitychange", refresh);
    return () => {
      active = false;
      window.removeEventListener("focus", loadBalances);
      document.removeEventListener("visibilitychange", refresh);
    };
  }, []);
  const data = usePeriodDashboardData(baseData, start, end);`,
  "carregar saldos líquidos no painel",
);

dashboard = replaceOnce(
  dashboard,
  `  const payoutFor = (memberId: number) => records.filter((item) => item.barberId === memberId).reduce((sum, item) => sum + barberPayoutCents(item), 0) + productSales.filter((item) => item.sellerTeamMemberId === memberId).reduce((sum, item) => sum + item.commissionCents, 0);`,
  `  const periodPayoutFor = (memberId: number) => records.filter((item) => item.barberId === memberId).reduce((sum, item) => sum + barberPayoutCents(item), 0) + productSales.filter((item) => item.sellerTeamMemberId === memberId).reduce((sum, item) => sum + item.commissionCents, 0);
  const payoutFor = (memberId: number) => teamMoneyRows.find((row) => row.teamMemberId === memberId)?.currentBalanceCents ?? periodPayoutFor(memberId);
  const photoFor = (memberId: number) => teamMoneyRows.find((row) => row.teamMemberId === memberId)?.photoUrl ?? null;`,
  "saldo líquido no painel",
);

const cardsBefore = `  const teamPayoutCards = data.team
    .filter((member) => member.id !== data.viewer.teamMemberId && member.active)
    .map((member) => ({ label: \`PAGAR \${member.name.toUpperCase()}\`, value: money(payoutFor(member.id)) }));
  const visibleTeamPayoutCards = showAllPayouts ? teamPayoutCards : teamPayoutCards.slice(0, 2);
  const hiddenPayoutCards = Math.max(0, teamPayoutCards.length - visibleTeamPayoutCards.length);
  const summaryCards = [
    ...visibleTeamPayoutCards,
    { label: \`COMISSÃO \${ownerName.toUpperCase()}\`, value: money(payoutFor(data.viewer.teamMemberId)) },
    { label: "SOBRA MENSALISTAS", value: money(activeMembership.revenueCents - activeMembership.payoutCents) },
  ];`;

const cardsAfter = `  const teamPayoutCards = data.team
    .filter((member) => member.active)
    .sort((left, right) => Number(left.id === data.viewer.teamMemberId) - Number(right.id === data.viewer.teamMemberId))
    .map((member) => ({
      memberId: member.id,
      name: member.name,
      label: \`PAGAR \${member.name.toUpperCase()}\`,
      valueCents: payoutFor(member.id),
      photoUrl: photoFor(member.id),
    }));
  const visibleTeamPayoutCards = showAllPayouts ? teamPayoutCards : teamPayoutCards.slice(0, 3);
  const hiddenPayoutCards = Math.max(0, teamPayoutCards.length - visibleTeamPayoutCards.length);`;

dashboard = replaceOnce(dashboard, cardsBefore, cardsAfter, "cards líquidos da equipe");

const summaryBefore = `    <section className="club-summary dynamic-summary">{summaryCards.map((item) => <article key={item.label}><small>{item.label}</small><strong>{item.value}</strong></article>)}</section>
    {teamPayoutCards.length > 2 && <div className="summary-expand"><button type="button" aria-expanded={showAllPayouts} onClick={() => setShowAllPayouts((current) => !current)}>{showAllPayouts ? "Ver menos comissões" : \`Ver mais comissões (+\${hiddenPayoutCards})\`}</button></div>}`;

const summaryAfter = `    <section className="team-payout-summary">
      <div className="team-payout-summary-head"><div><span><AppIcon name="members" /> PAGAMENTOS DA EQUIPE</span><p>Valores líquidos que cada um ainda precisa receber.</p></div><button type="button" onClick={() => go("Equipe")}>Ver detalhes →</button></div>
      <div className="team-payout-summary-grid">
        {visibleTeamPayoutCards.map((item) => <button type="button" className="team-payout-card" key={item.memberId} onClick={() => go("Equipe")}>
          <div className="team-payout-avatar">{item.photoUrl ? <img src={item.photoUrl} alt={\`Foto de \${item.name}\`} loading="lazy" /> : initials(item.name)}</div>
          <div><small>{item.label}</small><strong>{money(item.valueCents)}</strong><em>já descontado vale e fechamento</em></div>
        </button>)}
        <button type="button" className="team-payout-card monthly" onClick={() => go("Mensalistas")}><div className="team-payout-avatar"><AppIcon name="money" /></div><div><small>SOBRA MENSALISTAS</small><strong>{money(activeMembership.revenueCents - activeMembership.payoutCents)}</strong><em>saldo do período</em></div></button>
      </div>
    </section>
    {teamPayoutCards.length > 3 && <div className="summary-expand team-payout-expand"><button type="button" aria-expanded={showAllPayouts} onClick={() => setShowAllPayouts((current) => !current)}>{showAllPayouts ? "Ver menos saldos" : \`Ver mais saldos (+\${hiddenPayoutCards})\`}</button></div>}`;

dashboard = replaceOnce(dashboard, summaryBefore, summaryAfter, "visual de pagamentos da equipe");
writeFileSync(dashboardPath, dashboard);

const cssPath = "app/globals.css";
let css = readFileSync(cssPath, "utf8");
const marker = "/* Equipe: saldos líquidos, vales e fechamentos. */";
if (!css.includes(marker)) css += "\n/* Equipe: saldos líquidos, vales e fechamentos. */\n.team-payout-summary{margin:0 0 20px;padding:17px;border:1px solid #343c33;border-radius:17px;background:linear-gradient(145deg,#1c211c,#242b23);color:#f5f2e9;box-shadow:0 12px 26px #17201916}.team-payout-summary-head{display:flex;align-items:center;justify-content:space-between;gap:14px;margin-bottom:12px}.team-payout-summary-head>div{min-width:0}.team-payout-summary-head span{display:flex;align-items:center;gap:7px;color:#deb553;font-size:9px;font-weight:800;letter-spacing:.1em}.team-payout-summary-head span .app-icon{width:16px;height:16px}.team-payout-summary-head p{margin:5px 0 0;color:#b6beb4;font-size:10px;line-height:1.35}.team-payout-summary-head button{border:0;background:transparent;color:#e0b550;font-size:9px;font-weight:800;white-space:nowrap}.team-payout-summary-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:9px}.team-payout-card{min-width:0;display:flex;align-items:center;gap:9px;padding:11px;border:1px solid #3a4539;border-radius:12px;background:#252c25;color:#fff;text-align:left}.team-payout-card:hover{border-color:#6d633e}.team-payout-card>div:not(.team-payout-avatar){min-width:0;flex:1}.team-payout-card small,.team-payout-card strong,.team-payout-card em{display:block}.team-payout-card small{color:#bfc5bd;font-size:8px}.team-payout-card strong{margin-top:4px;color:#e4b853;font-size:15px;font-style:normal}.team-payout-card em{margin-top:3px;color:#8f9a8e;font-size:7px;font-style:normal;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.team-payout-avatar{width:34px;height:34px;flex:0 0 34px;border-radius:50%;overflow:hidden;display:grid;place-items:center;background:#e5c36d;color:#242419;font-size:9px;font-weight:800}.team-payout-avatar img{width:100%;height:100%;object-fit:cover}.team-payout-card.monthly .team-payout-avatar{background:#3a4338;color:#e4b853}.team-payout-card.monthly .team-payout-avatar .app-icon{width:18px;height:18px}.summary-expand.team-payout-expand{margin:-10px 0 20px}.summary-expand.team-payout-expand button{font-size:9px}\n.team-money-v2{display:grid;gap:15px}.team-money-v2-intro{display:flex;align-items:center;justify-content:space-between;gap:18px;padding:4px 2px 2px}.team-money-v2-intro>div{min-width:0}.team-money-v2-intro span{color:#9b7126;font-size:9px;font-weight:800;letter-spacing:.13em}.team-money-v2-intro h2{margin:6px 0 5px;font-family:Arial,Helvetica,sans-serif;font-size:25px;font-weight:720;letter-spacing:-.55px}.team-money-v2-intro p{max-width:650px;margin:0;color:#747b73;font-size:11px;line-height:1.48}.team-money-v2-tabs{display:flex;gap:9px;overflow-x:auto;padding:1px 1px 7px;scrollbar-width:thin}.team-money-v2-tabs button{min-width:155px;display:flex;align-items:center;gap:9px;padding:9px 11px;border:1px solid #e0dfd8;border-radius:12px;background:#fff;color:#2a2e29;text-align:left;box-shadow:0 2px 7px #25270c05}.team-money-v2-tabs button.active{border-color:#b78b34;background:#fffdf7;box-shadow:0 0 0 1px #d6ad55 inset}.team-money-v2-tabs button>span:not(.team-money-v2-avatar){min-width:0}.team-money-v2-tabs strong,.team-money-v2-tabs small{display:block}.team-money-v2-tabs strong{font-size:10px;font-weight:700;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.team-money-v2-tabs small{margin-top:3px;color:#888f87;font-size:8px;white-space:nowrap}.team-money-v2-avatar{width:38px;height:38px;flex:0 0 38px;border-radius:11px;object-fit:cover;background:#efe8d4}.team-money-v2-avatar.fallback{display:grid;place-items:center;background:linear-gradient(145deg,#dcb358,#f0d891);color:#28271f;font-size:10px;font-weight:800}.team-money-v2-avatar.large{width:50px;height:50px;flex-basis:50px;border-radius:15px;font-size:12px}.team-money-v2-focus{overflow:hidden;padding:18px}.team-money-v2-person{display:flex;align-items:center;gap:11px;margin-bottom:15px}.team-money-v2-person>div{min-width:0}.team-money-v2-person h2{margin:0;font-family:Arial,Helvetica,sans-serif;font-size:19px;font-weight:720;letter-spacing:-.3px}.team-money-v2-person small{display:block;margin-top:3px;color:#858b83;font-size:9px}.team-money-v2-status{margin-left:auto;padding:6px 9px;border-radius:999px;background:#e8f4ea;color:#397a4f;font-size:8px;font-weight:800}.team-money-v2-summary{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:9px}.team-money-v2-summary article{min-width:0;display:flex;align-items:flex-start;gap:9px;padding:13px;border:1px solid #ebe9e2;border-radius:11px;background:#fcfcfa}.team-money-v2-summary article>span{width:31px;height:31px;flex:0 0 31px;display:grid;place-items:center;border-radius:9px;background:#f4ead4;color:#a87824}.team-money-v2-summary article.receive>span{background:#e5f3e8;color:#348054}.team-money-v2-summary .app-icon{width:17px;height:17px}.team-money-v2-summary article>div{min-width:0}.team-money-v2-summary small,.team-money-v2-summary strong,.team-money-v2-summary em{display:block}.team-money-v2-summary small{color:#7e847c;font-size:7px;font-weight:800;letter-spacing:.04em}.team-money-v2-summary strong{margin-top:5px;font-size:15px;font-weight:750;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.team-money-v2-summary em{margin-top:4px;color:#949991;font-size:7px;font-style:normal;line-height:1.3}.team-money-v2-actions{display:flex;gap:9px;margin-top:14px}.team-money-v2-actions button{min-height:40px;display:inline-flex;align-items:center;justify-content:center;gap:7px;padding:9px 14px;border-radius:9px;font-size:9px;font-weight:800}.team-money-v2-actions .app-icon{width:15px;height:15px}.team-money-v2-actions .vale-action{border:1px solid #d9c18e;background:#fffaf0;color:#805d1d}.team-money-v2-actions .close-action{border:1px solid #c79734;background:#d9a741;color:#20241f}.team-money-v2-actions button:disabled{opacity:.45}.team-money-v2-day{display:flex;align-items:end;gap:8px;margin-top:12px;padding-top:12px;border-top:1px solid #efeee8}.team-money-v2-day label{display:grid;gap:5px;color:#767d75;font-size:8px}.team-money-v2-day input{width:92px;min-height:34px;border:1px solid #dfded6;border-radius:8px;background:#fff;padding:6px 8px;color:#30342f}.team-money-v2-day button{min-height:34px;border:1px solid #dedbd2;border-radius:8px;background:#f8f7f2;padding:6px 12px;color:#4d534d;font-size:8px;font-weight:800}.team-money-v2-vale-form,.team-money-v2-movements,.team-money-v2-history{padding:17px}.team-money-v2-heading{display:flex;align-items:flex-start;justify-content:space-between;gap:13px;margin-bottom:12px}.team-money-v2-heading span{color:#9a722a;font-size:8px;font-weight:800;letter-spacing:.12em}.team-money-v2-heading h3{margin:5px 0 0;font-family:Arial,Helvetica,sans-serif;font-size:16px;font-weight:720;letter-spacing:-.2px}.team-money-v2-heading>b{padding:5px 8px;border-radius:999px;background:#f2f0e7;color:#6d736c;font-size:8px}.team-money-v2-heading .quiet-close{border:0;background:#f4f2eb;border-radius:7px;padding:6px 9px;color:#656b64;font-size:8px}.team-money-v2-vale-form form{display:grid;grid-template-columns:150px 150px minmax(220px,1fr) auto;gap:10px;align-items:end}.team-money-v2-vale-form label{display:grid;gap:6px;color:#727970;font-size:8px;font-weight:700}.team-money-v2-vale-form input{width:100%;min-height:39px;padding:8px 10px;border:1px solid #deddd6;border-radius:9px;background:#fff;color:#272b27;font-size:10px}.team-money-v2-vale-form .primary-button{min-height:39px;padding:8px 14px}.team-money-v2-list{display:grid}.team-money-v2-list article{display:flex;align-items:center;gap:10px;min-height:54px;padding:8px 0;border-top:1px solid #efeee8}.team-money-v2-list article:first-child{border-top:0}.team-money-v2-list article>i{width:30px;height:30px;flex:0 0 30px;display:grid;place-items:center;border-radius:9px;font-style:normal}.team-money-v2-list article>i.vale{background:#edf2fa;color:#5279aa}.team-money-v2-list article>i.legacy{background:#f0eee7;color:#7e827b}.team-money-v2-list article>i .app-icon{width:15px;height:15px}.team-money-v2-list article>div:not(.team-money-v2-entry-actions){min-width:0;flex:1}.team-money-v2-list strong,.team-money-v2-list small{display:block}.team-money-v2-list strong{font-size:10px;font-weight:700}.team-money-v2-list small{margin-top:3px;color:#858b83;font-size:8px}.team-money-v2-list article>b{margin-left:auto;font-size:10px;color:#ad4747;white-space:nowrap}.team-money-v2-entry-actions{display:flex;gap:4px;margin-left:5px}.team-money-v2-entry-actions button{border:1px solid #e0ded6;border-radius:7px;background:#fff;padding:6px 7px;color:#626861;font-size:7px}.team-money-v2-entry-actions button.danger{color:#a54848;background:#fff8f8;border-color:#eed6d6}.team-money-v2-history-list article{display:flex;align-items:center;gap:10px;min-height:49px;padding:8px 0;border-top:1px solid #efeee8}.team-money-v2-history-list article:first-child{border-top:0}.team-money-v2-history-list article>div{min-width:0;flex:1}.team-money-v2-history-list strong,.team-money-v2-history-list small{display:block}.team-money-v2-history-list strong{font-size:10px;font-weight:700}.team-money-v2-history-list small{margin-top:3px;color:#858b83;font-size:8px}.team-money-v2-history-list article>b{font-size:10px;white-space:nowrap}.team-money-v2-history-list article>button{border:1px solid #dedcd4;border-radius:7px;background:#fff;padding:6px 8px;color:#765b23;font-size:8px;font-weight:800}.team-money-v2 .team-money-empty{margin:0;padding:22px 8px;color:#858b83;text-align:center;font-size:9px}.team-money-v2 .my-money-hero{font-family:Arial,Helvetica,sans-serif}\n@media(max-width:1000px){.team-payout-summary-grid,.team-money-v2-summary{grid-template-columns:repeat(2,minmax(0,1fr))}.team-money-v2-vale-form form{grid-template-columns:1fr 1fr}.team-money-v2-vale-form .reason-field{grid-column:1/-1}.team-money-v2-vale-form .primary-button{grid-column:1/-1}}\n@media(max-width:620px){.team-payout-summary{padding:14px;margin-bottom:16px}.team-payout-summary-head{align-items:flex-start}.team-payout-summary-head p{font-size:9px}.team-payout-summary-grid{grid-template-columns:1fr 1fr;gap:7px}.team-payout-card{padding:9px 8px;gap:7px}.team-payout-avatar{width:31px;height:31px;flex-basis:31px}.team-payout-card strong{font-size:13px}.team-payout-card em{font-size:6.5px}.team-money-v2{gap:12px}.team-money-v2-intro{padding:2px 1px}.team-money-v2-intro h2{font-size:21px}.team-money-v2-intro p{font-size:9px}.team-money-v2-tabs{margin-right:-11px;padding-right:11px}.team-money-v2-tabs button{min-width:137px;padding:8px}.team-money-v2-avatar{width:34px;height:34px;flex-basis:34px}.team-money-v2-focus{padding:14px}.team-money-v2-avatar.large{width:46px;height:46px;flex-basis:46px}.team-money-v2-person h2{font-size:17px}.team-money-v2-summary{grid-template-columns:1fr 1fr;gap:7px}.team-money-v2-summary article{padding:10px 8px;gap:7px}.team-money-v2-summary article>span{width:27px;height:27px;flex-basis:27px}.team-money-v2-summary strong{font-size:13px}.team-money-v2-actions{display:grid;grid-template-columns:1fr 1fr}.team-money-v2-actions button{width:100%;padding:8px}.team-money-v2-day{justify-content:space-between}.team-money-v2-vale-form,.team-money-v2-movements,.team-money-v2-history{padding:14px}.team-money-v2-vale-form form{grid-template-columns:1fr 1fr;gap:8px}.team-money-v2-vale-form .reason-field,.team-money-v2-vale-form .primary-button{grid-column:1/-1}.team-money-v2-list article{flex-wrap:wrap;gap:8px}.team-money-v2-list article>div:not(.team-money-v2-entry-actions){min-width:150px}.team-money-v2-entry-actions{width:100%;justify-content:flex-end;margin-left:38px}.team-money-v2-history-list article{flex-wrap:wrap}.team-money-v2-history-list article>div{min-width:180px}}\n";
writeFileSync(cssPath, css);

writeFileSync("tests/team-money-redesign-contract.test.mjs", "import test from \"node:test\";\nimport assert from \"node:assert/strict\";\nimport { readFile } from \"node:fs/promises\";\n\nconst read = (path) => readFile(new URL(`../${path}`, import.meta.url), \"utf8\");\n\ntest(\"saldo da equipe usa o mesmo ciclo de vales e fechamentos\", async () => {\n  const ledger = await read(\"db/team-money.ts\");\n  const dashboard = await read(\"app/ui/dashboard-app.tsx\");\n\n  assert.match(ledger, /currentBalanceCents = earnedCents - valeCents - paidCents/);\n  assert.match(ledger, /settlementCents = cycle\\.currentBalanceCents/);\n  assert.match(ledger, /lastDailyRecordId: cycle\\.lastDailyRecordId/);\n  assert.match(ledger, /lastProductSaleId: cycle\\.lastProductSaleId/);\n  assert.match(ledger, /lastTeamPaymentId: cycle\\.lastTeamPaymentId/);\n  assert.match(dashboard, /fetch\\(\"\\/api\\/team-money\"/);\n  assert.match(dashboard, /teamMoneyRows\\.find\\(\\(row\\) => row\\.teamMemberId === memberId\\)/);\n  assert.match(dashboard, /PAGAR \\$\\{member\\.name\\.toUpperCase\\(\\)\\}/);\n});\n\ntest(\"proprietário participa do controle e fotos cadastradas são reaproveitadas\", async () => {\n  const ledger = await read(\"db/team-money.ts\");\n  const ui = await read(\"app/ui/team-money-section.tsx\");\n\n  assert.match(ledger, /listPublicGalleryImages/);\n  assert.match(ledger, /photoUrl/);\n  assert.doesNotMatch(ledger, /filter\\(\\(member\\) => member\\.accessRole !== \"owner\"\\)/);\n  assert.doesNotMatch(ledger, /member\\.accessRole === \"owner\"\\) throw/);\n  assert.match(ui, /row\\.photoUrl/);\n  assert.match(ui, /CONTROLE DA EQUIPE/);\n});\n\ntest(\"nova interface cria somente vales; fechamento encerra o período\", async () => {\n  const ui = await read(\"app/ui/team-money-section.tsx\");\n\n  assert.match(ui, /kind: \"Vale\"/);\n  assert.match(ui, /Novo vale/);\n  assert.match(ui, /Fechar período/);\n  assert.match(ui, /próximo período começará zerado/);\n  assert.doesNotMatch(ui, /<option>Pagamento<\\/option>/);\n  assert.doesNotMatch(ui, /Novo pagamento/);\n});\n");
console.log("Redesign de vales e fechamentos aplicado.");
